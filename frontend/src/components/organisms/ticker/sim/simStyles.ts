import type React from 'react';

export const aiButtonStyle: React.CSSProperties = {
  background: 'var(--accent-bg)',
  border: '1px solid var(--accent-border)',
  color: 'var(--accent-color)',
  fontWeight: 600,
  transition: 'background 0.15s, box-shadow 0.15s',
};
